
export type SongType = {
    _id: string
    name: string
    favor: boolean
    count_seen: number
    time_last_seen: string
    hide: boolean
}

export type ListType = {
    _id: string
    name: string
}
